#!/usr/bin/env bash
for file in */*.tsv; do
    echo $file
    echo "$file"
done

if [ -f "filename.pdf" ]; then
    ./script.jl -a 3 --arg davs
elif [[ -n 4 ]]; then
    fisk subcommand ok
fi

brew install lars
sudo ls ~/hejsa.txt
ls *nice.pdf
# comment
echo "string"

mkdir "$hejsa"
